type Artist implements Node {
  alias: String!
  avatarId: String
  createdAt: DateTime!
  name: String
  genres: [String]
  id: ID!
  avatar: Image
  albums: [Album]
  tracks: [Track]
  featuresIn: [Track]
}

type Query {
  artist(id: ID!): Artist
  artists: [Artist]
}

type Mutation {
  createArtist(input: ArtistCreatePayload!): Artist
}

input ArtistCreatePayload {
  alias: String!
  name: String!
}
