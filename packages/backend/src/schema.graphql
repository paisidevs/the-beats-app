# import Query, Mutation from "./schema/Album/schema.graphql"
# import Query, Mutation from "./schema/Artist/schema.graphql"
# import Query, Mutation from "./schema/Playlist/schema.graphql"
# import Query, Mutation from "./schema/Track/schema.graphql"
# import Query, Mutation from "./schema/User/schema.graphql"

scalar Duration
scalar NonEmptyString

type Image implements Node {
  createdAt: DateTime!
  id: ID!
  url: String!
  uploadedBy: User!
}

enum ReleaseType {
  ALBUM
  EP
  LP
  PODCAST
  SINGLE
}

interface Node {
  id: ID!
}
